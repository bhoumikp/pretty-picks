import CartClient from "@/components/cart-client";

export const metadata = {
  title: "Cart",
  description: "Review your Pretty Picks cart.",
};

export default function CartPage() {
  return <CartClient />;
}
